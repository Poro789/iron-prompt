# Changelog

本项目所有重要变更均记录于此文件。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)（Semantic Versioning）。

## [1.3.1] - 2026-08-26

### Added

- **计时模式带练**：计时动作（拉伸/平板支撑等）现在也有 🎵 按钮——3s 准备 → 静态保持倒计时（每 5s 提示音，30/20/10s 语音提醒）→ 保持结束自动打卡并入休息

### Fixed

- **移动端「改变动作模式无响应」**：快速编辑弹窗超高时「保存微调」按钮被推出视口不可达，且点击屏幕下方命中遮罩会静默取消（无任何提示）——编辑弹窗卡片限高 85vh 可滚动，`.modal-center` 改为「能居中则居中、超高则顶部对齐可滚动」（`margin: auto` + 遮罩 `overflow-y: auto`），桌面端矮窗口同样受益
- **AI 处方 mode 变体丢失**：AI 返回 `"timer"` / `"TIME"` / `"hold"` / `"bodyweight"` 等变体时，模板的 `=== 'time'` 判断全部落空，界面表现为「模式没变」——新增 `Core.normalizeMode` 归一（无法识别的脏值保留原模式），AI 装载与快速编辑双向接入

## [1.3.0] - 2026-08-26

### Added

- 🖥️ **桌面版视图适配**（移动端体验不变）：
  - ≥640px 主列加宽至 480px，≥768px 加宽至 600px 并显示顶部品牌栏（版本号/数据隐私说明）
  - ≥1024px 双栏工作台：主列 726px + 320px 粘性侧栏（**大字号休息计时卡**（开始 90s / +15s / 停止）、**本次训练统计**（有效吨位/正式组进度/各阶段进度）、**AI 复盘内联面板**），全局字号 17px
  - ≥768px 全部底部抽屉改为居中对话框（负荷选择器一行 4 列）
  - 桌面快捷键：**R** 开始/停止组间休息（输入框聚焦时自动忽略），**Esc** 关闭弹窗
  - 悬停态（指针设备）、暗色滚动条、`color-scheme: dark`、桌面页脚（技术说明/快捷键/GitHub）
- **应用冒烟测试**：`tests/app.smoke.test.mjs` 用最小 Vue/DOM 桩真实执行 `setup()` 与全部关键业务路径（含首启迁移回归），纳入 CI

### Fixed

- **首启（空存档）崩溃**：初始计划原先不走存档迁移，缺少 `accessory`/`core` 阶段键，新用户首次粘贴含这两个阶段的 AI 处方、或在动作编排里给这两个阶段「+ 加动作」时抛 TypeError（现统一走 `migratePlans`，冒烟测试回归）
- **新训练日不停止带练**：重置打卡时进行中的节拍教练未停止，结束后会把刚清零的组重新打勾
- **备份校验不完整**：仅含单一日（缺 A 或缺 B）的备份可通过校验，还原后切换到缺失日导致界面崩溃；现校验要求 A/B 两日齐全
- AI 处方解析加固：`coach_review` 为数组时拒收（原会显示空看板）；小数节奏（如 `2.5s`）导入时取整，带练标题与倒计时一致

## [1.2.0] - 2026-08-26

> 基于全量代码审查（见 `REVIEW_REPORT.md`）的修复与增强版本。

### Added

- 🌅 **新训练日**：底部栏一键重置 A/B 两日全部打卡状态、吨位归零（二次确认，保留负荷/次数/RPE 记录），修复「本次训练总量」实为历史累计的问题
- **手动组间休息**：顶栏 ⏱ 按钮随时启动 90s 休息倒计时（原先只能在打卡后自动启动）
- **训练者档案**：配置中心可编辑体重 / 每日准备度，随 AI 复盘 Prompt 发送（原先恒为初始值）
- **快速编辑增强**：草稿模式（可取消、保存才生效），新增动作模式（哑铃/拉力绳/自重/计时）、节奏 3 段、目标发力、错误代警示等全字段编辑
- **空态引导**：当日无动作时给出添加动作 / 粘贴 AI 处方的引导
- **无障碍与可访问性**：图标按钮 aria-label、弹窗 role=dialog、Esc 关闭弹窗、允许放大页面、关键文本可选中复制、`prefers-reduced-motion` 支持
- **核心函数单元测试**：新增 `js/core.js` 纯函数模块（器械求解/AI 解析/存档迁移/吨位统计）与 `tests/core.test.mjs`（`node --test` 零依赖），GitHub Actions 增加 test job
- **PWA 级元信息**：favicon、meta description / OG、theme-color

### Changed

- 训练量统计口径：由仅 `strength` 阶段改为**全阶段有效正式组**（AI 把主项放进 accessory/core 时吨位不再被低估）
- 语音开关持久化（刷新不丢失），且同时控制提示音（🔇 = 全局静音）
- 计时器改为墙钟基准（`Date.now` 校准），长时间使用不再漂移；休息计时 250ms 刷新
- 计时模式（拉伸等）次数步进改为 5s（原先 1s 步进，180s 拉伸要点 180 下）
- 提示动画改为「淡入-保持-淡出」生命周期动画，不再无限闪烁
- 主按钮文案精简（「复制 AI 复盘 Prompt」），次级说明移入 title
- Vue 运行时升级 3.3.4 → 3.5.41（本地化）
- 部署工作流只发布运行所需文件（dist 过滤），不再把 `.github`/测试/文档发布到线上

### Fixed

- **提示音永久静音**：`playTone` 原先每次新建 AudioContext（约 6 次后超出浏览器上限全部失效），改为单例复用 + `resume()`
- **节拍教练「假完成」**：带练跑完原先只弹 RPE 面板、组未打勾、休息计时不启动（但语音已播「整组完成」）；现在真正完成打卡并自动进入休息；已完成的组不再提供带练（原先跑完会悄悄取消打卡）
- **拉力绳磅数编辑后负荷选项污染**：输入值被字符串拼接（出现 `015磅`、`1015磅` 等鬼选项并污染 AI 硬件约束）；数值统一归一，所有输入框改为 `v-model.number`
- **器械组合求解器指数爆炸**：极端库存下同步枚举曾实测冻结页面 21.8s；改为 DP 求解（有界背包/0-1 背包）+ 渲染上限，任意库存毫秒级返回
- **AI 处方解析**：阶段名大小写/空白归一（`Strength` 不再新建阶段块）；仅含 `coach_review` 的回复现在也有装载反馈；`overload_status` 缺字段时保留原值；空 `suggested_sets` 自动补默认正式组（不再产生永远无法完成的卡片）；解析失败改用 Toast（原 `alert` 阻塞）
- **备份还原**：增加二次确认与结构校验（失败给明确原因）；导入旧版本备份自动补阶段/档案结构（原先导入后「+加动作」「复制 Prompt」会报错）；同文件可连续重选；失败不再静默
- **CSS 工具类缺失 31 处**：桌面端弹窗拉满视口（`max-w-md/sm`）、完成卡片不置灰（`opacity-60`）、阶段头直角戳出圆角（`overflow-hidden`）、accessory/core 阶段标题失色、全部阴影/多处间距等全部补齐
- **localStorage 健壮性**：读取带结构校验（损坏存档回退默认计划）、写入防抖 + 异常捕获（隐私模式/配额满不再抛未捕获异常）、`pagehide` 时强制落盘
- 语音 `cancel→speak` 竞态丢字（延迟 speak）；`stopTempoCoach` 提前退出时取消进行中的语音；A/B 切换自动停止带练
- Toast 定时器竞争（新消息提前被旧定时器掐掉）；`URL.createObjectURL` 未回收
- 全局 `INITIAL_PLANS`/`DEFAULT_EQUIPMENT` 常量被 reactive 原地修改的隐患（深拷贝隔离）
- 剪贴板增加 `execCommand` 降级（非安全上下文）
- 顶栏 sticky 头部 4px 缝隙；全局默认字体下 `select` 控件样式缺失

## [1.1.0] - 2026-08-25

### Added

- 五阶段训练框架：新增 `accessory`（辅助强化与细节打磨）与 `core`（核心稳定与抗旋转）两个标准阶段，按规范顺序自动插入并重新编号
- 兼容未知阶段名：AI 处方返回未在规范内的 `phase` 时自动创建对应区块（A/B 两日同步补结构）
- 粘贴即装载：在「3. AI与数据」中粘贴完整且合法的处方 JSON 后自动解析装载，无需再手动点按钮（解析失败不打扰）
- 装载反馈：导入完成后 Toast 显示实际「更新 X 个动作 / 新增 X 个动作」数量

### Changed

- 主打卡视图仅渲染当前计划中有动作的阶段，空阶段不再占用版面；「1. 动作编排」Tab 中 5 个阶段全部可见、可编辑
- 旧版 `localStorage` 存档自动迁移：启动时为缺失的新阶段补齐空数组，数据不丢失

### Fixed

- **AI 处方解析器静默丢弃新动作**：原逻辑仅按名称覆盖已有动作，处方中的新增动作（如肩推、面拉、平板支撑、新增拉伸项等）全部被跳过 —— 现在同名覆盖 + 新动作追加
- **新阶段整体丢失**：处方中的 `accessory` / `core` 阶段原先因应用无对应结构而被整段丢弃
- 幽灵基准 `prev_record` 默认值：自重 / 计时 / 拉力绳模式下不再误显示为 `0kg×N`
- 空阶段不再误判为「全部完成」的绿色状态

## [1.0.0] - 2026-08-25

### Added

- IronPrompt Pro 首发版本：A/B 日计划切换、五感打卡流、组间休息计时器、沉浸式节拍带练、语音提示
- AI 复盘闭环：一键复制含硬件物理约束的复盘 Prompt，解析 AI 返回的 JSON 处方
- 自由器械算力：哑铃片/拉力绳库存物理合法重量求解与装铃指引
- JSON 全量备份/还原，`localStorage` 本地存储，纯静态零后端
